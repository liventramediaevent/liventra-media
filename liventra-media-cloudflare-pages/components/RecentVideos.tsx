import { YouTubeVideo } from "@/lib/youtube"; function formatDate(dateString:string){return new Intl.DateTimeFormat("en-US",{dateStyle:"medium"}).format(new Date(dateString));} export default function RecentVideos({videos}:{videos:YouTubeVideo[]}){return <section id="videos" className="container-main py-6"><div className="mb-5 flex items-center justify-between"><h2 className="section-title">Recent Videos</h2></div><div className="grid gap-6 sm:grid-cols-2 xl:grid-cols-4">{videos.map(video=><a key={video.id} href={`https://www.youtube.com/watch?v=${video.id}`} target="_blank" rel="noreferrer" className="glass-card group overflow-hidden rounded-3xl"><img src={video.thumbnail} alt={video.title} className="aspect-video w-full object-cover"/><div className="space-y-3 p-5"><h3 className="line-clamp-2 min-h-[3.2rem] text-base font-semibold leading-6 text-white">{video.title}</h3><p className="text-sm text-zinc-400">{formatDate(video.publishedAt)}</p></div></a>)}</div></section>}
